<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servicesnovigrad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ServiceApplicationForm" />
        <activity android:name=".ViewServices"></activity>
        <activity android:name=".ActivityBranch" />
        <activity android:name=".ManageEmployee" />
        <activity android:name=".ManageClients" />
        <activity android:name=".ModifyService" />
        <activity android:name=".EmployeeMenu" />
        <activity android:name=".AdminMenu" />
        <activity android:name=".AddServiceForm" />
        <activity android:name=".Accueil" />
        <activity android:name=".RegisterForm" />
        <activity android:name=".LoginForm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>